#ifndef GENERATE
#define GENERATE

#include <vector>
#include <string>

struct Course {
    std::string name;
    std::string id;
};

class Schedule {
    private:
        std::vector<Course> courses;

    public:

    
};

#endif